// import { Query } from './index';
// import { } from '../../interfaces';

// const all = () => Query<()[]>('');
// const one = (id: string) => Query<()[]>('', [id]);
// const insert = (newthing) => Query('INSERT INTO books SET ?', [newthing]);
// const update = (updatedX: {  }, id: string) => Query('UPDATE X SET ? WHERE id = ?', [updatedX, id]);
// const nuke = (id: string) => Query('DELETE from X WHERE books.id = ?', [id]);

// export default {
//     all,
//     one,
//     insert,
//     update,
//     nuke,
// }